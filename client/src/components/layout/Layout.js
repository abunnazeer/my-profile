import { Fragment } from 'react';
import Header from '../Header';
import MHeader from '../MHeader';
function Layout() {
  return (
    <Fragment>
      <Header />
      <MHeader />
    </Fragment>
  );
}

export default Layout;
